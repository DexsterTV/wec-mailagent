services:
  wec-mailing:
    image: ghcr.io/dexstertv/wec-mailagent:latest
    container_name: wec-mailing-agent
    ports:
      - "0.0.0.0:8004:3000"
    env_file: .env
    volumes:
      - wec_data:/app/data
    restart: unless-stopped

volumes:
  wec_data:
