{
  "name": "wec-mailing-agent",
  "version": "0.2.1",
  "description": "WEC Mailing Agent — narzędzie do generowania mailingu PR",
  "main": "server.js",
  "scripts": {
    "build": "npm --prefix ../frontend run build",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "google-auth-library": "^10.6.2"
  }
}
